# frozen_string_literal: true

source 'https://rubygems.org'

gem 'dotenv'
gem 'pry'
gem 'rack-cors', require: 'rack/cors'
gem 'rack-test'
gem 'sinatra'
gem 'sinatra-contrib'

gem 'groq', '~> 0.3.2'

group :test do
  gem 'rspec'
  gem 'vcr'
  gem 'webmock'
end
